To run our program compile it first, then run ProgramLauncher. If you want to log in as a organizer,
use "admin" as the username and password. If you want to exit the program use "/exit" program several times.

If you exit the program the proper way, the state of the data will be serialized into phase1/save/app.ser
If app.ser app exists, it will be loaded automatically.